<!DOCTYPE html>
<html lang="en">
  <head> 
    <style>
        #di{
            border: 3px solid black;
            padding: 20px;
        }
        #di a{
            text-decoration: none;
            border: 3px solid black;
        }
    </style>
  </head>
  <body>
    <div id="di">
        <a class="a1" href="#none">태그링크</a>
    </div>
    <script>
        //div, a 태그 객체 추출
        let di=document.getElementById('di')
        let a1=document.querySelector('#di a')

        //di 클릭 했을 때 고전이벤트 방식으로
        //테두리 색 blue

        di.onclick = function(){
            this.style.borderColor ='blue';
        }
        //a1 을 클릭해도 부모 div 가 blue로 변경됨
        // -> di에 클릭이벤트 -> a1는 di의 자식
        //이벤트가 부모인 div로 버블링 되면서 div 의 onclick이 실행된다.

        //버블링 : 이벤트가 이벤트 대상 요소에서 부터 시작 되어 부모 요소를 따라 전파되는것.
        a1.onclick = function(e){
            e.stopPropagation(); //a를 클릭해도 부모 div 이벤트가 실행되지 않는다. (버블링/캡쳐링 방지)
            this.style.borderColor ='red';
        }

    </script>
  </body>
</html>

